# Domain skills — overlay

Add project-specific domain skills here. Each skill captures context for a specific business domain,
integration, or technical area unique to your project.

The ticket router loads `environment-context` to match cards in `environments/`. Other domain skills
are loaded when the plan or implementation chat needs them.

See `skills/workflow/` for the core workflow skills (do not modify those unless upgrading core).
