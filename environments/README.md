# Environments — overlay

Add environment cards here: one file per environment or integration.

Each card describes how to run a specific environment locally — ports, commands, service dependencies,
gotchas. The ticket router loads the matched `environmentCard` from `profile.json` → `integrations`.

Start with `local-dev.md` and add integration cards as needed.
