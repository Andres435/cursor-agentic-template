---
name: worktrees
description: Overlay blank — how this project uses git worktrees. Fill in if profile.worktreeSupported is true.
keywords: worktree, parallel tickets, isolated checkout
---

# Worktrees (overlay)

**Stub.** Default `profile.worktreeSupported` is `false`. If you turn it on, document here:

- Where worktrees live (relative to this `.cursor` folder)
- How `/start-ticket --worktree` creates them
- How to open the ticket window

Until then, `/start-ticket` stays in **branch** mode.
